#include <stdio.h>
#include <stdlib.h>
#include <string.h>
                                                                                
#include <unistd.h>
#include <sys/mman.h>
                                                                                
#include <sys/types.h>
#include <sys/stat.h>
#include <fcntl.h>
                                                                                
int main(int argc, char *argv[]) 
{
  int i;
  int dllfd;
  unsigned long pos;

  unsigned char *dllbuf;

  struct stat status;
  off_t filesize;
  char *filename = argv[1];

  if (-1 == (i = stat(filename, &status))) {
    perror("stat");
    return -1;
  }
